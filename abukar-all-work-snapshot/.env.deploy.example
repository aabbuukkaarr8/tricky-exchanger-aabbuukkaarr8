# Скопировать на VPS: /opt/menyayka/.env
# cp .env.deploy.example .env && nano .env
#
# Вместе с .env на сервере должны лежать:
#   docker-compose.prod.yml, Caddyfile, backend/migrations/
# Их копирует GitHub Actions; для ручного подъёма — rsync/scp.

# --- Postgres ---
DB_USER=menyayka
DB_PASSWORD=CHANGE_ME_STRONG_DB_PASSWORD
DB_NAME=menyayka

# --- App ---
JWT_SECRET=CHANGE_ME_LONG_RANDOM_STRING
LOG_LEVEL=info
# Gin: в docker-compose.prod.yml принудительно GIN_MODE=release.
# Пустой = same-origin /api через Caddy (как в проде).
VITE_API_BASE_URL=

# --- SMTP (mail.ru app password) ---
# На Timeweb VPS исходящий SMTP mail.ru по IPv4 обычно закрыт, по IPv6 — ок,
# а Docker-сеть без IPv6. Поэтому на хосте крутится релей:
#   systemctl status menyayka-smtp-relay
#   (socat: 172.18.0.1:2525 plaintext -> mail.ru:465 TLS/IPv6)
# В .env для контейнера тогда:
SMTP_HOST=172.18.0.1
SMTP_PORT=2525
SMTP_USERNAME=menyayka_avito@mail.ru
SMTP_PASSWORD=
SMTP_FROM=menyayka_avito@mail.ru
SMTP_ENCRYPTION=plain

# --- MinIO ---
MINIO_ACCESS_KEY=CHANGE_ME_MINIO_ACCESS
MINIO_SECRET_KEY=CHANGE_ME_MINIO_SECRET
MINIO_BUCKET=items
# Публичные URL фото: https://menyayka.tech/s3/items/...
MINIO_PUBLIC_ENDPOINT=menyayka.tech/s3
MINIO_PUBLIC_USE_SSL=true

# --- Embeddings ---
EMBEDDING_PROVIDER=tei
VECTOR_DIM=384
TEI_IMAGE=ghcr.io/huggingface/text-embeddings-inference:cpu-1.9

# --- Matching (можно оставить дефолты) ---
MATCHING_THRESHOLD=0.5
CLUSTER_SIMILARITY_THRESHOLD=0.9

# Ranker: ml (дефолт) | formula
RANKER_MODE=ml

# --- Images (перезаписываются CI при деплое) ---
# --- Images ---
# CI при каждом деплое перезаписывает эти строки актуальным SHA-тегом.
# Не оставляй IMAGE_TAG=latest как «источник правды» для ручного compose up —
# иначе поднимутся не те образы, что выкатил Actions.
DOCKER_IMAGE_PREFIX=ghcr.io/avito-team-not-found/tricky-exchanger
IMAGE_TAG=latest
